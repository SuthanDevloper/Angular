import { BasictitlePipe } from './basictitle.pipe';

describe('BasictitlePipe', () => {
  it('create an instance', () => {
    const pipe = new BasictitlePipe();
    expect(pipe).toBeTruthy();
  });
});
