import { SamplecustomDirective } from './samplecustom.directive';

describe('SamplecustomDirective', () => {
  it('should create an instance', () => {
    const directive = new SamplecustomDirective();
    expect(directive).toBeTruthy();
  });
});
